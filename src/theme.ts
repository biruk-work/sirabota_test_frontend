import { createTheme } from '@material-ui/core'

export const theme = createTheme({
    palette: {
        primary: {
            main: '#ff5833',
        },
        secondary: {
            main: '#121212',
        },
        error: {
            main: '#ff5833',
        },
        background: {
            default: '#121212',
        },
    },
})
